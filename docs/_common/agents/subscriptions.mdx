By default, agents **won't** receive peers' audio streams.
This is by design and aims to protect the agents from unnecessary resource usage.

For an agent to start receiving a peer's audio, the peer must have the **subscribe** option set during **creation**.
